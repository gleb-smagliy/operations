{
  "name": "@operations/api.client",
  "version": "1.0.0",
  "main": "dist/ApiClient.js",
  "license": "MIT",
  "scripts": {
    "build": "rimraf dist && tsc",
    "generate": "cd ../api && yarn gen:client-ts --output ../api.client/__generated && cd ../api.client && yarn build"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.3.5"
  }
}
